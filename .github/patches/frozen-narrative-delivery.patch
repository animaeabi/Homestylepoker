diff --git a/supabase/functions/online-runtime-tick/index.ts b/supabase/functions/online-runtime-tick/index.ts
index f0eedd8..3333333 100644
--- a/supabase/functions/online-runtime-tick/index.ts
+++ b/supabase/functions/online-runtime-tick/index.ts
@@ -7,6 +7,7 @@ import { resolveCharacterStyle } from "../_shared/characters.ts";
 import { hasBanter, pickBanterLine, pickComebackLine } from "../_shared/bot_banter.ts";
 import { AMBIENT_BEATS, cannedReply, generateAmbientLine, generateGeminiReply, generateHandBanter, generateInnerThought, generateLlmReply, isPass, pickResponder } from "../_shared/bot_chat_reply.ts";
 import { generateSpeech } from "../_shared/bot_tts.ts";
+import { freezeNarrativeMoment, prepareNarrativeDelivery, withNarrativeMoment } from "../_shared/narrative_moment.js";
 import {
   advanceRunningJoke,
   classifySettle,
@@ -150,6 +151,7 @@ async function mixedHandBanter(opts: {
   memory?: string | null;
   mind?: string | null;
   recentSelf?: string[] | null;
+  moment?: any;
   canned: () => string | null;
 }): Promise<string | null> {
@@ -160,7 +162,7 @@ async function mixedHandBanter(opts: {
         apiKey: backend.apiKey,
         model: asText(Deno.env.get("CHAT_REPLY_MODEL")),
         speaker: { characterId: opts.speaker.characterId, groupPlayerId: "", name: opts.speaker.name, expressiveness: 1 },
-        situation: opts.situation,
+        situation: withNarrativeMoment(opts.situation, opts.moment),
         targetName: opts.targetName ?? null,
         roster: opts.roster,
         chatHistory: opts.chatHistory,
@@ -182,7 +184,7 @@ async function mixedHandBanter(opts: {
 // One private thought, broadcast-only (never persisted, never voiced): the
 // second voice layer. Best-effort; rate-limited by the caller.
 async function generateAndPostThought({
-  onlineClient, tableId, speaker, situation, memory, mind,
+  onlineClient, tableId, speaker, situation, memory, mind, moment,
 }: {
   onlineClient: ReturnType<typeof createOnlineRpcClient>;
   tableId: string;
@@ -190,6 +192,7 @@ async function generateAndPostThought({
   situation: string;
   memory?: string | null;
   mind?: string | null;
+  moment?: any;
 }): Promise<boolean> {
@@ -201,7 +204,7 @@ async function generateAndPostThought({
       apiKey: backend.apiKey,
       model: asText(Deno.env.get("CHAT_REPLY_MODEL")),
       speaker: { characterId: speaker.characterId, groupPlayerId: speaker.groupPlayerId, name: speaker.name, expressiveness: 1 },
-      situation,
+      situation: withNarrativeMoment(situation, moment),
       memory: memory ?? null,
       mind: mind ?? null,
@@ -211,6 +214,7 @@ async function generateAndPostThought({
       name: speaker.name,
       character: speaker.characterId,
       text,
+      moment,
     });
@@ -338,6 +342,43 @@ function createOnlineRpcClient() {
     return data;
   };
 
+  const resolveNarrativeDelivery = async ({
+    tableId,
+    message,
+    mood = null,
+    moment = null,
+  }: {
+    tableId: string;
+    message: string;
+    mood?: string | null;
+    moment?: any;
+  }) => {
+    if (!moment?.sourceHandId) {
+      return prepareNarrativeDelivery({ moment: null, text: message, mood });
+    }
+    try {
+      const { data: currentHand, error } = await client
+        .from("online_hands")
+        .select("id, state, hand_no")
+        .eq("table_id", tableId)
+        .order("hand_no", { ascending: false })
+        .limit(1)
+        .maybeSingle();
+      if (error) throw normalizeSupabaseError("[narrative current hand]", error);
+
+      let currentHasAction = false;
+      if (currentHand?.id && String(currentHand.state || "") === "preflop") {
+        const currentState = await callRpc("online_get_hand_state", {
+          p_hand_id: currentHand.id,
+          p_since_seq: null,
+        });
+        currentHasAction = Array.isArray(currentState?.events)
+          && currentState.events.some((event: any) => event?.event_type === "action_taken");
+      }
+      return prepareNarrativeDelivery({ moment, text: message, mood, currentHand, currentHasAction });
+    } catch (error) {
+      console.error("[narrative delivery] classify failed", error instanceof Error ? error.message : String(error));
+      return prepareNarrativeDelivery({ moment, text: message, mood, currentHand: { id: "unknown", state: "active" }, currentHasAction: true });
+    }
+  };
+
   return {
     client,
@@ -667,7 +708,8 @@ function createOnlineRpcClient() {
       voice,
       character,
       mood,
-      priority = 5
+      priority = 5,
+      moment = null,
     }: {
       tableId: string;
       groupPlayerId: string;
@@ -678,10 +720,15 @@ function createOnlineRpcClient() {
       character?: string | null;
       mood?: string | null;   // emotional delivery hint for TTS (win/lose/needle/...)
       priority?: number;      // conversation-director rank (1 strongest .. 6 weakest)
+      moment?: any;
     }) {
-      const trimmed = String(message || "").trim().slice(0, 178);
-      if (!trimmed) return;
+      const delivery = await resolveNarrativeDelivery({ tableId, message, mood, moment });
+      if (delivery.mode === "memory_only") return "memory_only";
+      const trimmed = String(delivery.text || "").trim().slice(0, 178);
+      const deliveryMood = delivery.mood || null;
+      if (!trimmed) return "memory_only";
 
       // Conversation director: a clearly weaker line must not stomp a strong
@@ -689,7 +736,7 @@ function createOnlineRpcClient() {
       // bluff kills the beat). Direct chains -- one rung apart -- flow through.
       const dirMem = await loadTableMemory(client, tableId);
-      if (stepsOnMoment(dirMem, priority)) return;
+      if (stepsOnMoment(dirMem, priority)) return "memory_only";
@@ -706,7 +753,7 @@ function createOnlineRpcClient() {
           (String(r.group_player_id) === mine || recent.indexOf(r) < 3)
         );
-        if (dup) return;
+        if (dup) return "memory_only";
@@ -714,7 +761,7 @@ function createOnlineRpcClient() {
         // alternate speakers.
         const lastMine = recent.find((r: any) => String(r.group_player_id) === mine);
-        if (lastMine?.created_at && Date.now() - new Date(lastMine.created_at).getTime() < 15000) return;
+        if (lastMine?.created_at && Date.now() - new Date(lastMine.created_at).getTime() < 15000) return "memory_only";
@@ -762,9 +809,11 @@ function createOnlineRpcClient() {
                 text: trimmed,
                 voice: Boolean(voice),
                 character: character || null,
-                mood: mood || null,
-                at: inserted?.created_at || new Date().toISOString()
+                mood: deliveryMood,
+                at: inserted?.created_at || new Date().toISOString(),
+                ...delivery.meta,
               }
@@ -773,6 +822,7 @@ function createOnlineRpcClient() {
         // live delivery is best-effort; the message is already persisted
       }
+      return delivery.mode;
     },
@@ -786,7 +836,8 @@ function createOnlineRpcClient() {
       name,
       character,
-      text
+      text,
+      moment = null,
     }: {
       tableId: string;
@@ -795,9 +846,13 @@ function createOnlineRpcClient() {
       name: string;
       character: string | null;
       text: string;
+      moment?: any;
     }) {
-      const trimmed = String(text || "").trim().slice(0, 140);
+      const delivery = await resolveNarrativeDelivery({ tableId, message: text, mood: "thought", moment });
+      if (delivery.mode !== "immediate") return;
+      const trimmed = String(delivery.text || "").trim().slice(0, 140);
       if (!trimmed) return;
@@ -819,7 +874,9 @@ function createOnlineRpcClient() {
               voice: true,
               character: character || null,
               mood: "thought",
-              at: new Date().toISOString()
+              at: new Date().toISOString(),
+              ...delivery.meta,
             }
@@ -1013,6 +1070,28 @@ async function runBotExpressions({
     const boardCards = Array.isArray(hand?.board_cards) ? hand.board_cards : [];
     const { events: memEvents, aftermath } = classifySettle({ players: settlePlayers, boardCards, potBb, handNo });
+    const sourceEventSeq = events.reduce((max: number, event: any) => Math.max(max, Number(event?.seq || 0)), 0);
+    const settleSummary = aftermath.kind === "hero_call"
+      ? `${aftermath.winnerName || "the caller"}'s hero call on ${aftermath.caughtName || "the bluffer"} in a ${Math.round(potBb)}bb pot`
+      : aftermath.kind === "bluff_called"
+        ? `${aftermath.caughtName || "the bluffer"}'s bluff getting shown in a ${Math.round(potBb)}bb pot`
+        : aftermath.kind === "cooler"
+          ? `${aftermath.winnerName || "the winner"} cooling ${aftermath.loserName || "the loser"} in a ${Math.round(potBb)}bb pot`
+          : `${aftermath.winnerName || "the winner"} taking a ${Math.round(potBb)}bb pot`;
+    const settleMoment = freezeNarrativeMoment({
+      sourceHandId: handId,
+      sourceEventSeq,
+      sourcePhase: "settle_result",
+      momentType: aftermath.kind,
+      street: boardCards.length >= 5 ? "river" : boardCards.length === 4 ? "turn" : boardCards.length >= 3 ? "flop" : "preflop",
+      potBb,
+      actor: { name: aftermath.winnerName || null, action: "win" },
+      opponent: { name: aftermath.caughtName || aftermath.loserName || null },
+      result: { winner: aftermath.winnerName || null, loser: aftermath.loserName || null, caughtBluff: aftermath.caughtName || null, winnerLabel: aftermath.winnerLabel || null, loserLabel: aftermath.loserLabel || null },
+      board: boardCards,
+      contextSummary: settleSummary,
+      callbackEligible: true,
+    }, { immediateMs: 18_000, callbackMs: 90_000 });
+    const settlePrivateMoment = freezeNarrativeMoment({ ...settleMoment, callbackEligible: false });
@@ -1184,6 +1263,7 @@ async function runBotExpressions({
           memory: memBlocksBuilt(speaker.characterId, speakerName),
           mind: speakerMind,
           recentSelf: avoid,
+          moment: settleMoment,
           canned: () => pickBanterLine({ characterId: speaker.characterId, context: cannedContext, targetName: null, avoid }),
         });
@@ -1191,7 +1271,7 @@ async function runBotExpressions({
         if (line) {
           spokenByCharacter = speaker.characterId;
-          await onlineClient.postBotChat({ tableId, groupPlayerId: speaker.groupPlayerId, message: line, voice: true, character: speaker.characterId, mood, priority: 2 });
+          await onlineClient.postBotChat({ tableId, groupPlayerId: speaker.groupPlayerId, message: line, voice: true, character: speaker.characterId, mood, priority: 2, moment: settleMoment });
@@ -1211,6 +1291,7 @@ async function runBotExpressions({
               memory: memBlocksBuilt(String(rival.botCharacter), String(rival.name || "Bot")),
               mind: mindLineFor(mem, String(rival.botCharacter)),
               recentSelf: rivalAvoid,
+              moment: settleMoment,
               canned: () => pickComebackLine({ characterId: String(rival.botCharacter), aboutName: speakerName, avoid: rivalAvoid }),
@@ -1220,7 +1301,7 @@ async function runBotExpressions({
             if (comeback) {
               await new Promise((resolve) => setTimeout(resolve, 300 + Math.floor(Math.random() * 400)));
-              await onlineClient.postBotChat({ tableId, groupPlayerId: rival.groupPlayerId, message: comeback, voice: true, character: String(rival.botCharacter), mood: "banter", priority: 3 });
+              await onlineClient.postBotChat({ tableId, groupPlayerId: rival.groupPlayerId, message: comeback, voice: true, character: String(rival.botCharacter), mood: "banter", priority: 3, moment: settleMoment });
@@ -1253,6 +1334,7 @@ async function runBotExpressions({
           await onlineClient.postBotChat({
             tableId, groupPlayerId: bot.groupPlayerId, message: nv,
             voice: vocal, character: nvPick.characterId, mood: "nonverbal", priority: 3,
+            moment: settlePrivateMoment,
           });
@@ -1285,6 +1367,7 @@ async function runBotExpressions({
             situation: thoughtSituation,
             memory: memBlocksBuilt(thoughtPick.characterId, thoughtPick.name),
             mind: mindLineFor(mem, thoughtPick.characterId),
+            moment: settlePrivateMoment,
           });
@@ -1715,9 +1798,28 @@ async function processBotAction({
     });
   }
 
+  const narrativeBb = Math.max(1, Number(table?.big_blind || 2));
+  const narrativeStreet = String(liveHand?.state || hand.state || "preflop");
+  const narrativePotBb = Number(liveHand?.pot_total || 0) / narrativeBb;
+  const narrativeAmountBb = decision.amount != null ? Number(decision.amount) / narrativeBb : Number(toCall || 0) / narrativeBb;
+  const narrativeActorName = String(character?.name || actingSeat?.group_players?.name || "Bot");
+  const sourceEventSeq = (handState?.events || []).reduce((max: number, event: any) => Math.max(max, Number(event?.seq || 0)), 0) + 1;
+  const midHandMomentBase = {
+    sourceHandId: hand.id,
+    sourceEventSeq,
+    sourcePhase: "mid_hand_action",
+    momentType: `${narrativeStreet}_${decision.actionType}`,
+    createdAt: Date.now(),
+    street: narrativeStreet,
+    potBb: narrativePotBb,
+    actor: { name: narrativeActorName, action: decision.actionType, amountBb: narrativeAmountBb },
+    board: Array.isArray(liveHand?.board_cards) ? liveHand.board_cards : [],
+    callbackEligible: true,
+  };
+
   // Mid-hand table talk -- DETACHED: characters flash lines/thoughts after a
@@ -1776,6 +1878,14 @@ async function processBotAction({
         const memMid = target?.name ? await loadTableMemory(onlineClient.client, tableId) : null;
         const spareTarget = Boolean(target?.name && memMid
           && needledTooRecently(memMid, String(target.name)) && Math.random() < 0.7);
         if (target?.name && memMid && !spareTarget) {
+          const midHandMoment = freezeNarrativeMoment({
+            ...midHandMomentBase,
+            opponent: { name: target.name },
+            contextSummary: `${narrativeActorName}'s ${decision.actionType} on ${target.name} on the ${narrativeStreet} into a ${Math.round(narrativePotBb)}bb pot`,
+          }, { now: midHandMomentBase.createdAt, immediateMs: 8_000, callbackMs: 60_000 });
           const priceBb = Math.max(raiseToBbForChat, 0);
@@ -1792,6 +1902,7 @@ async function processBotAction({
             memory: memoryPromptBlock(memMid, { speakerCharacterId: String(actingSeat.bot_character), speakerName: String(character.name || "Bot") }),
             mind: mindLineFor(memMid, String(actingSeat.bot_character)),
             recentSelf: avoidFor(actingSeat.group_player_id),
+            moment: midHandMoment,
             canned: () => pickBanterLine({
@@ -1804,12 +1915,11 @@ async function processBotAction({
               avoid: avoidFor(actingSeat.group_player_id),
             }),
           });
-          if (line && Date.now() - narrativeStartedAt <= 8000) {
-            noteNeedle(memMid, String(target.name));
-            saveTableMemory(onlineClient.client, tableId, memMid);
-            await onlineClient.postBotChat({
+          if (line) {
+            const deliveryMode = await onlineClient.postBotChat({
               tableId,
@@ -1820,7 +1930,12 @@ async function processBotAction({
               mood: "needle",
               priority: 3,
+              moment: midHandMoment,
             });
+            if (deliveryMode === "memory_only") return;
+            noteNeedle(memMid, String(target.name));
+            saveTableMemory(onlineClient.client, tableId, memMid);
@@ -1846,6 +1961,7 @@ async function processBotAction({
                   roster,
                   chatHistory: [...history, { name: String(speakerName), text: line }],
+                  moment: midHandMoment,
                   canned: () => pickComebackLine({
@@ -1862,6 +1978,7 @@ async function processBotAction({
                     mood: "needle",
                     priority: 4,
+                    moment: midHandMoment,
                   });
@@ -1902,6 +2019,11 @@ async function processBotAction({
       const mem = await loadTableMemory(onlineClient.client, tableId);
       const streetForThought = String(liveHand?.state || hand.state || "the hand");
+      const thoughtMoment = freezeNarrativeMoment({
+        ...midHandMomentBase,
+        contextSummary: `${narrativeActorName}'s ${decision.actionType} on the ${narrativeStreet} in a ${Math.round(narrativePotBb)}bb pot`,
+        callbackEligible: false,
+      }, { now: midHandMomentBase.createdAt, immediateMs: 8_000, callbackMs: 8_000 });
@@ -1924,6 +2046,7 @@ async function processBotAction({
         situation: `Facing ${toCallBbForThought.toFixed(0)}bb on the ${streetForThought}, you just ${actionWord}. The action is done and public. The private thought you had while deciding -- the read, the doubt, the self-coaching, or the thing you'd never admit.`,
         memory: memoryPromptBlock(mem, { speakerCharacterId: String(actingSeat.bot_character), speakerName: String(character.name || "Bot") }),
         mind: mindLineFor(mem, String(actingSeat.bot_character)),
+        moment: thoughtMoment,
       });
